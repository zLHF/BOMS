# Changelog

## 2026-06-04 - 交付前检查与补强

### Added

- 新增 `.env.example`，集中列出本地和生产部署所需环境变量占位。
- 新增 `DEPLOYMENT.md`，补充服务器要求、环境变量、数据库初始化、Docker 构建、上线后健康检查和常见问题。
- 新增 `TESTING.md`，说明后端/前端测试命令、构建验证和核心业务冒烟范围。
- 新增 `PRE_LAUNCH_CHECKLIST.md`，提供上线前代码、测试、数据库、环境变量、安全、性能、回滚和上线后验证清单。
- 新增批量导入文件校验单元测试。

### Changed

- 后端 `application.yml` 改为优先读取环境变量，保留本地开发默认值。
- 批量导入入口增加文件名、扩展名、空文件和大小上限校验。
- 批量导入任务改为复制上传内容后异步执行，避免请求线程被 Excel 解析阻塞以及临时文件生命周期问题。
- 前端批量导入模板下载改为复用统一 token 读取方法，并处理未登录/非 2xx 响应。
- README 增加交付文档入口和上线前注意事项。

### Security

- 标记生产环境必须替换 JWT/数据库/MinIO 密钥并关闭演示明文密码登录。
- 降低非 Excel 文件或超大文件进入导入解析链路的风险。
